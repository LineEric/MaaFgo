# MaaFgo 自动战斗：路线图

> **版本**：v1.2
> **日期**：2026-08-01
> **分支**：`feat/auto-battle-planner`
> **配套文档**：`MaaFgo_自动战斗技术方案_整合版.md`（设计）、`MaaFgo_自动战斗_架构与实现.md`（代码现状）

---

## 现状小结

**V1b 已通过真机验证** ✅。核心闭环（主界面→开卡→选3张→等动画→下一回合→胜利）已跑通：契约/决策器/校验器（纯逻辑完整）、感知/执行/回合循环均正常工作。ROI 坐标、识别节点（`自动战斗_感知.json`）已创建并验证。当前以 `time.sleep` 间隔代替后置确认，真机条件下稳定可用。

**待补全（非阻塞，提升鲁棒性/完整性）**：后置确认逻辑、参数解析（`custom_action_param` 未实际使用）、敌方槽位坐标、Pipeline 入口节点、save_evidence。

---

## 阶段总览

| 阶段 | 一句话目标 | 能打什么 | 主要阻塞 |
|---|---|---|---|
| **V1** ✅ | 平A + 无脑宝具跑通最小闭环 | 不叠 buff 也能清的本 | ~~后置确认 + 模板图 + 真机验证~~ 已完成 |
| **V2** | 按固定攻略打真正的三回合周回本 | 绝大多数周回本 | 技能感知/执行 + 数据 |
| **V2 补充** | Chaldea 配队/攻略导入 | 免手写攻略 | 复用现有 chaldea 模块 |
| **集成/鲁棒（并行）** | 无人值守连刷 | — | 结算/弹窗需截图 |
| **V3+** | LLM 决策 / 伤害模拟 | 高难/未知本 | 需 V2 结构化事实齐备 |

---

## V1 ✅ 已完成

**目标**：让现有骨架真机跑通"主界面→开卡→选3张→等动画→下一回合→胜利"。

**已完成**
- ✅ ROI 坐标标定：5 面卡 ROI、上排 NP 卡 ROI/点击点、攻击钮坐标均已填入并验证。
- ✅ 识别节点创建：`自动战斗_感知.json` 含卡色 OCR、NP 数值 OCR、场景 TemplateMatch/OCR，均验证通过。
- ✅ 场景模板图片制作并验证。
- ✅ 真机验证：核心闭环跑通，能通过不需技能的本。

**待补全（非阻塞，可随 V2 一起做）**
- 后置确认逻辑（当前以 `time.sleep` 代替，真机可用）。
- 参数解析（`custom_action_param` 未实际使用）。
- Pipeline 入口节点 `自动战斗.json`。
- 主动选敌人目标（需标定 `ENEMY_POINT` 坐标 + 敌人识别节点）。
- `save_evidence` 失败存证据。

---

## V2：按固定攻略打周回本

**主题**：从"只会平A+宝具"升级到"能按攻略放技能三宝具清场"。这是让工具真正有用的关键跳跃（现在周回基本都要先叠 buff）。

### A. 主动技能（从者技能 + 御主技能）—— V2 核心
- 感知：技能可用/CD、**技能目标选择子屏**、御主技能。
- 执行：`cast_servant_skill(slot, idx, target_ally)`、`cast_master_skill(...)`，处理目标子屏后置确认。
- 契约：启用 `BattleAction.servant_skills / master_skills`（已预留字段）。

### B. 固定回合计划 TurnPlan（FGA 式）
- 每面预设"放哪些技能给谁、宝具顺序、是否换人"；选卡仍动态。
- **关键**：用固定计划则**无需理解技能效果**——策略由计划编码，程序只按计划点。伤害模拟/效果理解留到 V3。
- 契约新增 `TurnPlan / BattlePlan`；决策器按 TurnPlan 出技能序列 + 动态选卡。

### C. 按角色（卡归属识别）
- `owner_slot` 识别（模板或轻量 NN 分类）。
- 解锁：Brave chain、指定从者优先、计划按角色引用。

### D. 选目标 / 宝具顺序 / 换人
- 主动选目标（若 V1 未完成则并入）。
- 宝具顺序优化（先辅助后输出）。
- 换人 order change（三换队常用）。

**V2 内部建议顺序**：A+B（能按攻略打本）→ D（多敌与三换）→ C（Brave/按角色优化）。

**依赖**：从者/技能元数据（目标类型等，判断是否弹目标子屏）；真实截图标定技能/目标 UI。

---

## V2 补充项：Chaldea 配队接入

**目标**：把队伍名单、乃至每回合操作攻略从 Chaldea 导入，省掉手写 TurnPlan。

- 复用现有 `agent/chaldea/`（现用于配 BBC）。新增一个"转成 TeamSnapshot / TurnPlan"的转换器，与 `chaldea_converter.py`（转 BBC）并列。
- **两层数据**：
  - 名单（谁/站位/礼装/魔术礼装/等级）→ `TeamSnapshot` → 支撑 owner_slot、Brave、按角色。
  - 操作序列（若数据含）→ 直接转 `TurnPlan` → 免手写攻略。
- **铁律**：Chaldea 是先验计划，运行时仍以画面为准（实际抽卡/NP/存活由感知定）。
- **License**：仅用数据（项目已在用），不混入 AGPL 代码。
- **待查**：现有 chaldea 模块究竟解析了"操作序列"还是仅"名单"，决定能白嫖多少。

---

## 集成 / 鲁棒性轨（与 V2 并行）

让它能无人值守连刷，多为"需截图才能做"：
- **结算流程（#7）**：打完通关/掉落/点击继续到回主界面；原生路径尚未开发，需截图；可查 BBC 路径 `战斗完成信息` 能否复用。
- **进场动画 >15s（#4）**：首个未知等待超时可能误判卡死，需放宽/区分。
- **开场攻略提示、关卡机制提示弹窗（#5/#6）**：需"点一下继续"，当前会被当异常。
- **宝具封印可用态识别**：封印时 NP 卡出现但点不动；当前靠"变化确认"安全中止（打不过不乱来），要打过封印本需感知区分可用/封印态（需封印截图）。
- pipeline 集成：把 `auto_battle` 接入可调度入口（独立按钮 → custom action → 未来 pipeline 节点）。

---

## V3+：LLM 与模拟（远期）

规则 + 固定计划的 V2 稳定、且卡归属/结构化事实齐备后，才引入：
- `LLMDecider`：在本地生成的合法候选中做高层取舍 / 解释 / 离线复盘；接口与 `RuleDecider` 相同，替换即可。
- 伤害/NP 估值模拟：为决策提供清场概率等估值。
- 这才是"大模型接管"的合适时机——那时模型有可靠事实可推理，而非盲看截图。

---

## 依赖与"需截图"标注

| 事项 | 需真实 720p 截图？ | 状态 |
|---|---|---|
| ~~V1 场景模板制作、坐标验证~~ | ~~是~~ | ✅ 已完成 |
| ~~V1 后置确认逻辑（像素差/识别确认）~~ | ~~否~~ | 待补（当前 sleep 代替） |
| ~~V1 参数解析、Pipeline 入口节点~~ | ~~否~~ | 待补 |
| V1 选目标标定 | 是 | 待补 |
| V2 技能 CD/目标子屏/御主技能识别 | 是 | — |
| V2 卡归属 owner_slot | 是（正常态；封印态另需封印图） | — |
| V2 TurnPlan 结构、决策器逻辑 | 否（纯逻辑，可先写） | — |
| Chaldea 转换器（名单/攻略→TeamSnapshot/TurnPlan） | 否（取决于数据，可先写） | — |
| 回合循环可测性改造 + Fake 测试 | 否 | — |
| 结算 / 弹窗 / 进场超时 | 是 | — |

---

## 附：各阶段模块改动映射

| 模块 | V1 ✅ 已完成 | V2 |
|---|---|---|
| `core/models` | ✅ 完整 | 启用 servant_skills/master_skills/order_change；加 TurnPlan/BattlePlan/TeamSnapshot |
| `core/decider` | ✅ 完整 | 读 TurnPlan 出技能序列 + 动态选卡 |
| `perception` | ✅ 已验证（OCR 卡色/NP/场景） | 加技能 CD/可用、技能目标子屏、御主技能、卡归属 |
| `execution` | ✅ 已验证（坐标/点击）；后置确认待补 | 加 cast_servant_skill/cast_master_skill/order_change + 确认 |
| `runtime` | ✅ 已验证；参数解析待补 | 主界面分支加"选目标 + 放技能" |
| `custom` | ✅ 已验证；Pipeline 入口/save_evidence 待补 | — |
| `data` | ⬜ 空 | 从者/技能元数据、TeamSnapshot |
| `chaldea` | — | 新增转 TeamSnapshot/TurnPlan 的转换器 |
